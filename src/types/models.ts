type Lesson = {
    id: string,
    lessonName: string,
    color: string,
    imageUri: string
}

type UserLesson = {
    id: string,
    lessonName: string,
    color: string,
    imageUri: string,
    progress: number,
}